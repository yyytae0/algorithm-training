/*
#include <stdio.h>

int main() {
	int a, b, cnt=0;
	int lst[100];
	scanf("%d", &a);

	for (int i = 0; i < a; i++)
	{
		scanf("%d ", &lst[i]);
	}
	scanf("%d", &b);
	for (int i = 0; i < a; i++)
	{
		if (lst[i] == b)
		{
			cnt++;
		}
	}
	printf("%d", cnt);
	return 0;
}
*/