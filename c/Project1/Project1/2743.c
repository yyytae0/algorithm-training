/*
#include <stdio.h>

int main() {
	int n, ans, idx=0;
	char str[101];
	scanf("%s", str);
	ans = count(str);
	printf("%d", ans);
	return 0;
}

int count(char *str) {
	int idx=0;
	while (str[idx] != 0)
	{
		idx++;
	}
	return idx;
}
*/