/*
#include <stdio.h>

int main() {
	int n, m, a, b, c;
	int lst[101];
	scanf("%d %d", &n, &m);
	for (int i = 0; i < n; i++)
	{
		lst[i] = 0;
	}
	for (int i = 0; i < m; i++)
	{
		scanf("%d %d %d", &a, &b, &c);
		for (int j = a-1; j < b; j++)
		{
			lst[j] = c;
		}
	}
	for (int i = 0; i < n; i++)
	{
		printf("%d ", lst[i]);
	}
	return 0;
}
*/