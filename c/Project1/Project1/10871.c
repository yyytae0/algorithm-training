/*
#include <stdio.h>

int main() {
	int a, b, cnt=0;
	int lst[10000];
	scanf("%d %d", &a, &b);
	for (int i = 0; i < a; i++)
	{
		scanf("%d", &lst[i]);
	}
	for (int i = 0; i < a; i++)
	{
		if (lst[i] < b)
		{
			printf("%d ", lst[i]);
		}
	}

	return 0;
}
*/