/*
#include <iostream>
using namespace std;

int main() {
	int n, x, y, mxx=-10000, mnx=10000, mxy=-10000, mny=10000;
	cin >> n;
	for (int i = 0; i < n; i++)
	{
		cin >> x >> y;
		if (x > mxx)
		{
			mxx = x;
		}
		if (x < mnx)
		{
			mnx = x;
		}
		if (y > mxy)
		{
			mxy = y;
		}
		if (y < mny)
		{
			mny = y;
		}
	}
	cout << (mxx - mnx)*(mxy - mny);
	return 0;
}
*/