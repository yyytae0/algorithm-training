/*
#include <stdio.h>

int main() {
	int n, d;
	char str[1001];
	scanf("%d", &n);
	for (int i = 0; i < n; i++)
	{
		scanf("%s", str);
		d = count(str);
		printf("%c%c\n", str[0], str[d - 1]);
	}
}

int count(char *str) {
	int idx=0;
	while (str[idx] != 0)
	{
		idx++;
	}
	return idx;
}
*/